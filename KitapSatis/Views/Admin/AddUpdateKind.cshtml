 @model Kind
@{
    var Title = "Tür Oluştur";
}
  

        <form method="post">
          <div asp-validation-summary="ModelOnly" class="text danger"></div>
          <input type="hidden" asp-for="KindId"/>
          @if(Model.KindId !=0)
            {
                <p class="fs-3">Tür Güncelle</p>
                Title = "Tür Güncelle";
            }
            <div class="col-12 border-bottom text-center">
                <h2 class="text-primary">@Title</h2>
                </div>
            <div class="form-group row">
                <div class="">
                            <input asp-for="KindName" placeholder="Ürün isim" class="form-control" />
                            <span asp-validation-for="KindName" class="text-danger"></span>
                </div>

              
            </div>
             <div class="form-group row">
                                <div class="col-4 offset-4">
                            @if(Model.KindId != 0)
                            {
                                <button type="submit" class="btn btn-primary">Güncelle</button>
                            }
                            else
                            {
                                <button type="submit" class="btn btn-primary">Ekle</button>
                            }
                        </div>
                            <a asp-action="category" class="btn-primary btn-success form-control">Geri Dön</a>


                            </div>
          
        </form>








   